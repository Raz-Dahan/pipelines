apiVersion: v1
kind: Pod
metadata:
  name: "redis-response-test"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: redis-test
      image: redis
      command: ["sh", "-c"]
      args:
        - "redis-cli -h redis -p 6379 ping"
  restartPolicy: Never
